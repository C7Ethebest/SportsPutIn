server:
  port: 8888

#数据库数据源
spring:
  datasource:
#    type: com.alibaba.druid.pool.DruidDataSource
    password: root
    username: root
    url: jdbc:mysql://localhost:3306/SportsPunchIn?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver

#日志
logging:
  file:
    name: log/log
  level:
    root: info
    lileixn: debug


file:
  # 文件上传目录
  uploadFolder: D:\DATA\
  # 对外访问的资源目录
  staticAccessPath: /image/**

